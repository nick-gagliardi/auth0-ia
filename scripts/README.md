# Scripts

- `pnpm build:index` builds the docs index into `./index/`.

Environment variables:
- `DOCS_REPO_URL` (default: https://github.com/auth0/docs-v2.git)
- `DOCS_REPO_REF` (default: main)
- `WORKDIR` (default: .work/docs-v2)
- `OUT_DIR` (default: index)
